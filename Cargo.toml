[package]
name = "pdf_faktura"
version = "0.1.0"
edition = "2021"

[dependencies]
printpdf = "0.7"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true